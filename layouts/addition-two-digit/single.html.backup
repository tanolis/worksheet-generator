<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ .Title }} - Worksheet Generator</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Comic+Neue:wght@400;700&family=Nunito:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #3A86FF;
            --secondary: #FB5607;
            --accent: #FF006E;
            --success: #38B000;
            --dark: #1D3557;
        }
        
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Nunito', sans-serif; background: #F0F8FF; color: var(--dark); }
        
        .container { max-width: 1200px; margin: 0 auto; padding: 0 20px; }
        
        /* Header */
        header {
            background: linear-gradient(135deg, var(--primary), #5FA8FF);
            color: white;
            padding: 1.5rem 0;
            border-bottom: 5px solid var(--accent);
        }
        
        .header-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .logo h1 { font-family: 'Comic Neue', cursive; font-size: 2rem; }
        .nav-links a { color: white; text-decoration: none; margin-left: 20px; font-weight: 600; }
        
        /* Main Content */
        .main-content { padding: 3rem 0; }
        
        .page-header {
            text-align: center;
            background: white;
            padding: 2rem;
            border-radius: 20px;
            margin-bottom: 3rem;
            box-shadow: 0 8px 25px rgba(58, 134, 255, 0.1);
            border: 3px dashed var(--accent);
        }
        
        .page-header h1 {
            font-family: 'Comic Neue', cursive;
            font-size: 2.8rem;
            color: var(--primary);
            margin-bottom: 1rem;
        }
        
        /* Generator */
        .generator-container {
            display: grid;
            grid-template-columns: 300px 1fr;
            gap: 30px;
            margin-bottom: 40px;
        }
        
        @media (max-width: 992px) {
            .generator-container { grid-template-columns: 1fr; }
        }
        
        .settings-panel {
            background: white;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
            border: 3px solid var(--secondary);
        }
        
        .setting-group { margin-bottom: 25px; }
        .setting-group h3 { color: var(--secondary); margin-bottom: 15px; }
        
        .option-btn {
            width: 100%;
            padding: 12px;
            margin: 5px 0;
            background: #FFF0F5;
            border: 2px solid #FFE0EB;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
        }
        
        .option-btn.active {
            background: var(--secondary);
            color: white;
            border-color: var(--secondary);
        }
        
        /* Worksheet Display */
        .worksheet-display {
            background: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
            border: 3px solid var(--primary);
        }
        
        .worksheet-title {
            text-align: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 3px dotted var(--accent);
        }
        
        .worksheet-title h2 {
            font-family: 'Comic Neue', cursive;
            color: var(--primary);
            font-size: 2.2rem;
        }
        
        .addition-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 30px;
        }
        
        .addition-problem {
            background: #FFF9F0;
            padding: 25px;
            border-radius: 10px;
            font-size: 1.8rem;
            text-align: center;
            border-left: 5px solid var(--success);
        }
        
        .problem-number {
            display: inline-block;
            background: var(--accent);
            color: white;
            width: 35px;
            height: 35px;
            border-radius: 50%;
            text-align: center;
            line-height: 35px;
            font-weight: bold;
            margin-right: 20px;
        }
        
        /* Vertical addition formatting */
        .vertical-problem {
            display: inline-block;
            text-align: right;
            margin: 0 20px;
        }
        
        .vertical-problem div {
            padding: 5px 20px;
        }
        
        .line {
            border-top: 2px solid #333;
            margin: 5px 0;
        }
        
        /* Action Buttons */
        .action-buttons {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 40px;
        }
        
        .action-btn {
            padding: 15px 30px;
            border: none;
            border-radius: 10px;
            font-size: 1.1rem;
            font-weight: 700;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .generate-btn { background: var(--primary); color: white; }
        .print-btn { background: var(--secondary); color: white; }
        .back-btn { background: var(--dark); color: white; }
        
        .action-btn:hover { transform: translateY(-3px); box-shadow: 0 8px 20px rgba(0,0,0,0.15); }
        
        /* Footer */
        footer {
            background: var(--dark);
            color: white;
            padding: 3rem 0;
            margin-top: 4rem;
        }
        
        @media print {
            .no-print { display: none !important; }
            .addition-grid { grid-template-columns: repeat(2, 1fr) !important; }
        }
    </style>
</head>
<body>
    <header class="no-print">
        <div class="container header-container">
            <div class="logo">
                <h1><i class="fas fa-plus-square"></i> {{ .Title }}</h1>
            </div>
            <nav class="nav-links">
                <a href="/">🏠 Home</a>
                <a href="/addition-single-digit/">1️⃣ Single Digit</a>
                <a href="/addition-three-digit/">3️⃣ Three Digit</a>
            </nav>
        </div>
    </header>

    <main class="main-content">
        <div class="container">
            <div class="page-header no-print">
                <h1><i class="fas fa-calculator"></i> Two Digit Addition Generator</h1>
                <p>Practice adding numbers 10-99 with or without regrouping. Build mental math skills!</p>
            </div>

            <div class="generator-container">
                <div class="settings-panel no-print">
                    <h2><i class="fas fa-sliders-h"></i> Worksheet Settings</h2>
                    
                    <div class="setting-group">
                        <h3><i class="fas fa-list-ol"></i> Number of Problems</h3>
                        <button class="option-btn active" onclick="setProblems(8)">8 Problems</button>
                        <button class="option-btn" onclick="setProblems(12)">12 Problems</button>
                        <button class="option-btn" onclick="setProblems(16)">16 Problems</button>
                        <button class="option-btn" onclick="setProblems(20)">20 Problems</button>
                    </div>
                    
                    <div class="setting-group">
                        <h3><i class="fas fa-chart-line"></i> Difficulty Level</h3>
                        <button class="option-btn active" onclick="setDifficulty('easy')">Easy (No regrouping)</button>
                        <button class="option-btn" onclick="setDifficulty('medium')">Medium (Some regrouping)</button>
                        <button class="option-btn" onclick="setDifficulty('hard')">Hard (All regrouping)</button>
                    </div>
                    
                    <div class="setting-group">
                        <h3><i class="fas fa-th-large"></i> Problem Type</h3>
                        <button class="option-btn active" onclick="setType('vertical')">Vertical Format</button>
                        <button class="option-btn" onclick="setType('horizontal')">Horizontal Format</button>
                        <button class="option-btn" onclick="setType('mixed')">Mixed Practice</button>
                    </div>
                    
                    <div class="setting-group">
                        <h3><i class="fas fa-check-circle"></i> Options</h3>
                        <label style="display:block; margin:10px 0;">
                            <input type="checkbox" id="includeAnswers" checked> Include Answer Key
                        </label>
                        <label style="display:block; margin:10px 0;">
                            <input type="checkbox" id="showSteps"> Show Carrying Steps
                        </label>
                    </div>
                </div>

                <div class="worksheet-display">
                    <div class="worksheet-title">
                        <h2>Two Digit Addition Practice</h2>
                        <p>Name: __________________ Date: _______________</p>
                    </div>
                    
                    <div class="addition-grid" id="problemsContainer">
                        <!-- Problems will be generated here -->
                    </div>
                    
                    <div id="answerKey" style="margin-top:40px; padding:20px; background:#E8F7F6; border-radius:10px; display:none;">
                        <h3><i class="fas fa-key"></i> Answer Key</h3>
                        <div id="answersContainer"></div>
                    </div>
                </div>
            </div>

            <div class="action-buttons no-print">
                <button class="action-btn generate-btn" onclick="generateWorksheet()">
                    <i class="fas fa-magic"></i> Generate New Worksheet
                </button>
                <button class="action-btn print-btn" onclick="window.print()">
                    <i class="fas fa-print"></i> Print Worksheet
                </button>
                <a href="/" class="action-btn back-btn">
                    <i class="fas fa-arrow-left"></i> Back to Home
                </a>
            </div>
        </div>
    </main>

    <footer class="no-print">
        <div class="container">
            <p style="text-align:center;">© 2023 Worksheet Generator - Mastering Two Digit Addition!</p>
        </div>
    </footer>

    <script>
        let settings = {
            problems: 8,
            difficulty: 'easy',
            type: 'vertical',
            includeAnswers: true,
            showSteps: false
        };
        
        function setProblems(num) {
            document.querySelectorAll('[onclick^="setProblems"]').forEach(btn => btn.classList.remove('active'));
            event.target.classList.add('active');
            settings.problems = num;
            generateWorksheet();
        }
        
        function setDifficulty(level) {
            document.querySelectorAll('[onclick^="setDifficulty"]').forEach(btn => btn.classList.remove('active'));
            event.target.classList.add('active');
            settings.difficulty = level;
            generateWorksheet();
        }
        
        function setType(type) {
            document.querySelectorAll('[onclick^="setType"]').forEach(btn => btn.classList.remove('active'));
            event.target.classList.add('active');
            settings.type = type;
            generateWorksheet();
        }
        
        function generateWorksheet() {
            const container = document.getElementById('problemsContainer');
            const answerContainer = document.getElementById('answersContainer');
            const answerKey = document.getElementById('answerKey');
            
            container.innerHTML = '';
            answerContainer.innerHTML = '';
            
            // Update options
            settings.includeAnswers = document.getElementById('includeAnswers').checked;
            settings.showSteps = document.getElementById('showSteps').checked;
            
            // Generate problems
            for (let i = 1; i <= settings.problems; i++) {
                let num1, num2;
                
                // Generate appropriate numbers based on difficulty
                switch(settings.difficulty) {
                    case 'easy': // No regrouping
                        num1 = Math.floor(Math.random() * 90) + 10;
                        num2 = Math.floor(Math.random() * (99 - num1 % 10)) + 1;
                        break;
                    case 'medium': // Some regrouping
                        if (Math.random() > 0.5) {
                            // With regrouping
                            num1 = Math.floor(Math.random() * 90) + 10;
                            num2 = Math.floor(Math.random() * 9) + (10 - (num1 % 10));
                        } else {
                            // Without regrouping
                            num1 = Math.floor(Math.random() * 90) + 10;
                            num2 = Math.floor(Math.random() * (9 - (num1 % 10))) + 1;
                        }
                        break;
                    case 'hard': // All regrouping
                        num1 = Math.floor(Math.random() * 90) + 10;
                        num2 = Math.floor(Math.random() * 9) + (10 - (num1 % 10));
                        break;
                }
                
                const sum = num1 + num2;
                let problemHTML = '';
                let stepsHTML = '';
                
                // Calculate carrying steps if needed
                const ones1 = num1 % 10;
                const tens1 = Math.floor(num1 / 10);
                const ones2 = num2 % 10;
                const tens2 = Math.floor(num2 / 10);
                
                const onesSum = ones1 + ones2;
                const carry = onesSum >= 10 ? 1 : 0;
                const finalOnes = onesSum % 10;
                const finalTens = tens1 + tens2 + carry;
                
                if (settings.showSteps && carry > 0) {
                    stepsHTML = `
                        <div style="font-size:0.9rem; color:#666; margin-top:10px;">
                            <strong>Steps:</strong> ${ones1} + ${ones2} = ${onesSum} (carry ${carry})<br>
                            ${tens1} + ${tens2} + ${carry} = ${finalTens}<br>
                            Answer: ${finalTens}${finalOnes}
                        </div>
                    `;
                }
                
                switch(settings.type) {
                    case 'vertical':
                        problemHTML = `
                            <div class="vertical-problem">
                                <div>${num1}</div>
                                <div>+ ${num2}</div>
                                <div class="line"></div>
                                <div>______</div>
                            </div>
                            ${stepsHTML}
                        `;
                        break;
                    case 'horizontal':
                        problemHTML = `${num1} + ${num2} = ______ ${stepsHTML}`;
                        break;
                    case 'mixed':
                        if (Math.random() > 0.5) {
                            problemHTML = `
                                <div class="vertical-problem">
                                    <div>${num1}</div>
                                    <div>+ ${num2}</div>
                                    <div class="line"></div>
                                    <div>______</div>
                                </div>
                                ${stepsHTML}
                            `;
                        } else {
                            problemHTML = `${num1} + ${num2} = ______ ${stepsHTML}`;
                        }
                        break;
                }
                
                container.innerHTML += `
                    <div class="addition-problem">
                        <span class="problem-number">${i}</span>
                        ${problemHTML}
                    </div>
                `;
                
                // Add to answer key
                if (settings.includeAnswers) {
                    answerContainer.innerHTML += `
                        <div style="margin:5px 0; padding:8px; background:white; border-radius:5px;">
                            ${i}. ${num1} + ${num2} = <strong>${sum}</strong>
                        </div>
                    `;
                }
            }
            
            // Show/hide answer key
            answerKey.style.display = settings.includeAnswers ? 'block' : 'none';
            
            // Show success message
            if (!window.printing) {
                alert(`✅ Generated ${settings.problems} two-digit addition problems!`);
            }
        }
        
        // Initialize
        document.addEventListener('DOMContentLoaded', generateWorksheet);
        
        // Update on checkbox change
        document.getElementById('includeAnswers').addEventListener('change', generateWorksheet);
        document.getElementById('showSteps').addEventListener('change', generateWorksheet);
    </script>
</body>
</html>