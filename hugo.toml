baseURL = "https://tanolis.github.io/worksheet-generator/"
languageCode = "en-us"
title = "Worksheet Generator"

# IMPORTANT: This tells Hugo to build directly to root
publishDir = "docs"

[outputs]
  home = ["HTML"]
  page = ["HTML"]

# This helps with GitHub Pages
[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true